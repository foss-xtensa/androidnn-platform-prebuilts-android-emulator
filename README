This directory contains prebuilt Android 14 emulator Linux x86-64 binaries for QEMU 34.1.3 that were generated by
running the following command on a 64-bit Linux machine:

  external/qemu/android/rebuild.sh

inside Android emu-34-release package.

Patches to support co-simulation with Cadence Xtensa XTSC simulator are applied
